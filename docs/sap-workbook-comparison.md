# SAP 与总表 PO 比对

异常与待办、数据中心现在按总表当前值核对 SAP PO、订单行及物料。PO 与订单行不一致分别生成异常；总表行号空白或为占位符时单列为“总表订单行缺失”，双方都有行号且数值不同才生成订单行不一致；缺少唯一 SAP 参考关联时生成待关联事项，不推断为 SAP 中不存在该订单。

- 旧工作区先按不可变记录 ID 读取原导入批次的 SAP 关联，然后读取 Firebase `sap/current` 指向的 PO 表。每 15 秒检查新快照，只有指针变化才重新下载 PO 表；校验行数、快照标识和唯一 PO／行键后原子更新参考字段。无需覆盖原工作区或重新初始化 Firebase。
- 保留已确认的 SAP PO／行关联；没有关联时优先按原始 PO／行精确匹配，否则仅在同 PO、料号、数量有唯一候选时关联。多个候选不猜选。仅唯一候选符合 Excel 行号 × 10 = SAP 行号、且不存在 Excel 原行号对应的 SAP 行时，识别为顺序编号。双方原值仍保留，其他行号差异仍生成异常。
- 自动关联的原始依据、SAP 行及编号规则存入工作区 `sapReferenceLinks/{generation}`，使用 ETag 防止并发覆盖。重新打开网页仍沿用对应关系；仅修改料号或数量不会额外制造行号差异。关联保存失败时不采用未保存的结果，保留上次成功参考并提示。
- Excel 侧来自当前 `importFields.po` 和 `importFields.line`。空白、`/`、`—`、`-` 都按缺失处理；纯数字标识忽略前导零和两端空白，但展示与导出保留原文。多 PO 文本不会擅自选择其中一个作为匹配结果。
- 不因 PO 编辑重新关联其他源记录。已完成、已取消行也参与比对。新增总表记录先显示待关联，不擅自用同料号猜测 SAP 订单。
- 异常与待办默认显示「当前待办」，「历史记录」包含总表剩余待发为零及取消记录，「全部记录」可查看全部范围。剩余待发为零不代表 SAP 已收货。导航徽标使用当前待办数，筛选和导出跟随所选范围；历史记录和跟进不删除。
- 修改保存后使用现有共享工作区机制重新计算异常。修正一致后异常自动关闭，再次变化会重新打开并保留跟进记录。仅填写备注不能关闭 PO 差异。读取参考数据失败时显示读取失败事项，已有 PO 差异不会误判通过；刷新页面可以重试读取。
- 异常列表以独立列并列显示双方 PO、订单行和料号，同时存在的 PO、料号差异都会高亮。六个表头均有 Filter 按钮，使用表头下拉搜索、多选、跨列叠加和清除筛选；筛选不更改业务记录。异常搜索支持双方 PO、订单行和料号；订单详情与导出均保留双方字段。异常 Excel 为 22 列，按当前筛选导出全部结果。
- 原导入的 `sap_link`、`material`、`price` 警告已由动态规则替代，避免修正后旧初始化警告仍保持未处理。价格仍有独立动态事项，未直接视为通过。

## 验证与边界

2026-09-11 通过现有 Firebase 只读加载验证：1,253 行、当前 PO 快照 8,031 行；1,125 行具有 SAP 参考匹配、128 行仍待关联、0 行读取失败。原 460 行中有 332 行恢复关联，其中 307 行属于顺序编号，25 行仍为真实行号差异。剩余待关联原因：74 行多候选、29 行该 PO 下未找到料号、21 行无正式 PO、4 行订单行或数量需核对。

本次检测共 487 条事项：当前待办 120 条（价格 99、发运承诺 10、料号 9、其他初始化 1、数量 1），历史记录 367 条（订单行缺失 151、待关联 128、料号 51、订单行差异 25、共用 SAP 行 12）。共用行不计为新增订单或重复累计数量。这些是检查时的数据，后续编辑及时间变化会改变数量。

自动化覆盖导入适配、旧批次读取、前导零与空值、PO/行修改、完成记录、跟进与自动关闭、参考失败、Firebase 加载及总表更新联动、导出双方字段和界面筛选。全部测试使用本地模拟写入；云端仅做读取。

本次接入最新快照中的 PO 身份、料号、单位与价格参考；收货、在途、SO、冲销及分批数量尚未完整投影到工作区。新身份关联不会自动认定数量单位可比，也不向 SAP 写回总表编辑。读取失败保留上次成功参考并在网页提示。

## 未来四份 Excel 的输入边界

沿用主表、未完成清单、周发运、延期四种结构。准备脚本支持变化的日期文件名和任意主表行数；同角色有多个文件时拒绝猜选，可用 `--master`、`--open`、`--weekly`、`--delayed` 指定这次四个文件。每种表检查原有表头顺序，缺列或移列时停止导入，避免错读料号、PO、数量。补充表多次发运仍是原订单的观察记录；未唯一关联的补充记录作为独立待办展示和导出，不加入订单数量。

这不是任意格式自动识别，也没有取消既有工作区的覆盖保护：本轮未导入新文件、未增加网页四文件上传入口或自动替换整个历史工作区。新批次正式生效仍需通过导入流程，不能用旧行号强行合并新旧文件。正常 SAP 快照更新由网页自动核对。
